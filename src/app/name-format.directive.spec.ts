import { NameFormatDirective } from './name-format.directive';

describe('NameFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new NameFormatDirective();
    expect(directive).toBeTruthy();
  });
});
